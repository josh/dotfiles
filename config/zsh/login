# shellcheck source=/dev/null
source ~/.config/sh/login

if [ -f "$HOME/.nix-profile/etc/zprofile" ]; then
	source "$HOME/.nix-profile/etc/zprofile"
fi

DOTFILES=$(cd "$(dirname "${0:A}")/../.." && pwd)
export DOTFILES
