[user]
	name = Joshua Peek
	email = josh@joshpeek.com
[github]
	user = josh
[core]
	excludesfile = /Users/josh/.gitignore
[alias]
	co = checkout
	ct = checkout -t
	ci = commit -v
	ca = commit -av
	st = status -sb
	up = pull
	track = !git-track
	prune-all = !git-prune-all
	fetch-all = !git-fetch-all
	maintenance = !git-maintenance
	pull-auto = !git checkout master && git reset --hard && git pull
[apply]
	whitespace = nowarn
[branch]
	autosetupmerge = true
[color]
	branch = auto
	diff = auto
	status = auto
[push]
	default = tracking
[rerere]
	enabled = 1
