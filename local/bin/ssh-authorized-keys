#!/usr/bin/env bash

set -euo pipefail

curl \
	--fail \
	--show-error \
	--silent \
	--output "$HOME/.ssh/authorized_keys~" \
	"https://github.com/josh.keys"
chmod 600 "$HOME/.ssh/authorized_keys~"
mv "$HOME/.ssh/authorized_keys~" "$HOME/.ssh/authorized_keys"
