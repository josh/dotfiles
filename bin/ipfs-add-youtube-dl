#!/bin/bash

set -euf -o pipefail
set -x

rm -rf /tmp/youtube-dl
mkdir -p /tmp/youtube-dl
pushd /tmp/youtube-dl

youtube-dl --write-description --write-thumbnail "$1"

ipfs add --recursive .

popd
rm -rf /tmp/youtube-dl
